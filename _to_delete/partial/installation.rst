Installation
============

RefET can be installed with pip:

.. code-block:: console

    pip install refet

or from conda-forge:

.. code-block:: console

    conda install conda-forge::refet

The only runtime dependency is `NumPy <https://numpy.org>`__ (>=2.0).
Python 3.11 or newer is required.
