"""Data access modules for weather station networks.

These modules have dependencies beyond NumPy (install with
``pip install refet[data]``) and are imported explicitly, e.g.::

    from refet.io import agrimet

"""
